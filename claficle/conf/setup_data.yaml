defaults:
  - _self_
  - data: oscar
  # https://stackoverflow.com/a/70777327/9889508
  - override hydra/hydra_logging: none
  - override hydra/job_logging: none

seed: 1

disable_wandb: False
lang: null
tokenizer_name: null  # name of tokenizer in checkpoints/tokenizers/
checkpoint_dir: ${oc.env:TMPDIR}/repos/claficle/checkpoints/

# to avoid hydra creating output dirs: https://stackoverflow.com/a/64635492/9889508
hydra:
  run:
    dir: .
  output_subdir: null
