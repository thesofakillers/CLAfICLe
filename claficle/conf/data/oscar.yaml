# configuration for the OSCAR dataset
defaults:
  - _self_
  - defaults # see claficle/conf/data/defaults.yaml

sample_size_mb: 4096
val_percent: 0.005

# to avoid hydra creating output dirs: https://stackoverflow.com/a/64635492/9889508
hydra:
  run:
    dir: .
  output_subdir: null
