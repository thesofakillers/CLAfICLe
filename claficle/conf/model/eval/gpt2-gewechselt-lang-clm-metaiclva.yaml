# WECHSELized GPT2 with a MetaICL adapter, trained in the standard way

base_checkpoint: "metaicl_Gewechselt_{$LANG}-clm-v{$SEED}.ckpt"

defaults:
  - base_model
  - _self_
name: "vessel"
tokenizer_name: "{LANG}_gewechselt"
adapter_checkpoint: "metaiclva-v{$SEED}"
adapter_name: "metaicl"
