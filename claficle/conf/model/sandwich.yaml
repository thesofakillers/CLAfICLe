name: "sandwich"
causalLM_variant: "gpt2-large"
base_checkpoint: "checkpoints/metaicl.pt"
pl_checkpoint: null
extra_proc_fn: "translate"

# to avoid hydra creating output dirs: https://stackoverflow.com/a/64635492/9889508
hydra:
  run:
    dir: .
  output_subdir: null
